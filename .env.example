# Apotex Dashboard — Environment Variables
# Copy to .env and fill values before running

# Gemini AI Assistant (optional — dashboard works without it)
VITE_GEMINI_API_KEY=your_gemini_api_key_here
VITE_GEMINI_MODEL=gemini-2.0-flash

# Production server port (for vite preview)
PORT=4173
